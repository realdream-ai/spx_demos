onStart => {
	hide
}

onClick => {
	hide
	broadcast StartGame
}

onMsg PlantsWin, => {
	levelOver = true
	level += 1
	if level < 6 {
		show

		repeatUntil backdropName() != "植物赢了", => {
			if touching(Mouse) {
				setEffect BrightnessEffect, 20
			} else {
				setEffect BrightnessEffect, 0
			}
		}
	}
}
